esphome:
  name: esp32_aio
  friendly_name: ESP32 AIO Sensor
  min_version: 2025.6.0
  name_add_mac_suffix: true

esp32:
  board: esp32-s3-devkitm-1
  framework:
    type: esp-idf

wifi:

api:

logger:

ota:
  - platform: esphome
    id: ota_esphome

time:
  - platform: homeassistant
    id: homeassistant_time 

web_server:
  local: true
